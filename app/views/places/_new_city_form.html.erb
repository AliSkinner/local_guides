<%= form_for(@city) do |f| %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <!-- <div class="field">
    < %= f.text_field :latitude %>
  </div>
  <div class="field">
    < %= f.text_field :longitude %>
  </div> -->
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
